/**
 * == (Loose Equality)  -Value compare + type conversion
 * ex-  "5" == 5 → true, [] == false → true
 * 
 * 
 * === (Strict Equality)    -Value + type compare
 * ex-  "5" === 5 → false, [] === false → false
 */
